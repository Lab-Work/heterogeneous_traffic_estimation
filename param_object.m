classdef param_object
    properties 
        test_time
        test_dist
        d_t
        d_x
        rho_c % critical density
        rho_max % jam density
        v_max % maximum velocity
        q_max % maximum flow
        rho_l % upstream
        rho_r % downstream
    end
end